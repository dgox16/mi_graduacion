---
import FormularioNiños from "../components/FormularioNiños.astro";

import { listaQuimicos } from "../constants/quimicos";
---

<section
    class="flex items-center justify-center mt-6 md:mt-16"
    id="confirmarAsistencia"
>
    <div
        class="bg-black p-5 sm:p-8 md:p-9 lg:p-14 rounded-lg w-[280px] xs:w-[350px] sm:w-[600px] lg:w-[800px] 2xl:w-[1100px]"
    >
        <div class="text-center text-pretty">
            <h1
                class="text-lg xs:text-xl sm:text-2xl lg:text-4xl text-white font-bold uppercase"
            >
                Confirme su asistencia
            </h1>
            <h2
                class="text-white opacity-95 text-xs xs:text-sm sm:text-base lg:text-lg"
            >
                Nos encantaría contar con su presencia en este día especial. Por
                favor, confirme su asistencia llenando el siguiente formulario.
            </h2>
        </div>
        <form
            class="w-full mt-9 sm:mt-12"
            action="/api/asistenciaSupabase"
            method="post"
        >
            <div class="flex flex-wrap -mx-3 mb-5 md:mb-6">
                <div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
                    <label
                        class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
                        for="nombre"
                    >
                        Nombre
                    </label>
                    <input
                        class="appearance-none block w-full text-sm sm:text-base bg-gray-200 rounded py-2 sm:py-3 px-4 -mb-1 md:mb-3 leading-tight focus:outline-none focus:bg-white"
                        id="nombre"
                        name="nombre"
                        type="text"
                        placeholder="Coloca tu nombre"
                    />
                </div>
                <div class="w-full md:w-1/2 px-3">
                    <label
                        class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
                        for="apellidos"
                    >
                        Apellidos
                    </label>
                    <input
                        class="appearance-none block w-full bg-gray-200 text-sm sm:text-base text-gray-700 border border-gray-200 rounded py-2 sm:py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
                        id="apellidos"
                        name="apellidos"
                        type="text"
                        placeholder="Escribe tus apellidos"
                    />
                </div>
            </div>
            <div class="flex flex-wrap -mx-3 mb-6">
                <div class="w-full px-3">
                    <label
                        class="block uppercase tracking-wide text-white text-xs font-bold mb-2"
                        for="quimico"
                    >
                        ¿A cual de nuestros quimicos farmacobiologos acompañará?
                    </label>
                    <select
                        class="text-sm sm:text-base block appearance-none w-full text-gray-700 py-2 sm:py-3 px-4 pr-8 rounded leading-tight focus:outline-none focus:bg-white focus:border-gray-500"
                        id="quimico"
                        name="quimico"
                    >
                        {
                            listaQuimicos.map((quimico) => (
                                <option value={quimico.id}>
                                    {quimico.nombre}
                                </option>
                            ))
                        }
                    </select>
                </div>
            </div>
            <FormularioNiños />
            <div class="flex justify-end">
                <button
                    type="submit"
                    class="bg-yellow-400 px-5 py-3 rounded-lg w-full md:w-24"
                    >Enviar</button
                >
            </div>
        </form>
    </div>
</section>
